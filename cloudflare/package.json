{
  "name": "aforo-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev --port 8787",
    "migrate:local": "wrangler d1 migrations apply aforo-db --local",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "miniflare": "4.20260730.0",
    "wrangler": "4.135.0"
  },
  "overrides": {
    "undici": "^7.29.0",
    "sharp": "^0.35.4"
  }
}
